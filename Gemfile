source ENV['GEM_SOURCE'] || 'https://rubygems.org'

group :development do
  gem 'rake'
  gem 'rainbow'
  gem 'puppet', ENV['PUPPET_VERSION'] || '~> 4.2.1'
  gem 'puppet-lint'
  gem 'puppet-syntax'
end
